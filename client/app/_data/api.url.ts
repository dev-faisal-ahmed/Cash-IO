export const serverAddress = 'http://localhost:5000/api/v1';

export const apiUrl = {
  // auth
  register: `${serverAddress}/auth/register`,
  login: `${serverAddress}/auth/login`,
  googleLogin: `${serverAddress}/auth/login/google`,
};
