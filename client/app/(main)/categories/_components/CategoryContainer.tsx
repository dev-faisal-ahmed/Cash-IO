'use client';

import * as card from '@/components/ui/card';

type TProps = {};

export const CategoryContainer = ({}: TProps) => {
  return (
    <card.Card>
      <card.CardHeader>
        <card.CardTitle></card.CardTitle>
      </card.CardHeader>
    </card.Card>
  );
};
